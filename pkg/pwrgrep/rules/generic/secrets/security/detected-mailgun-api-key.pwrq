# rules: detected-mailgun-api-key
# from: generic/secrets/security/detected-mailgun-api-key.yaml

. as $root
| ( ($root | scan_regex(["*"]; [
      "key-[0-9a-zA-Z]{32}"
    ])) as $all
    | ( $all | of(["key-[0-9a-zA-Z]{32}"])
        | finding("detected-mailgun-api-key";
            "Mailgun API Key detected") ) )
| report
