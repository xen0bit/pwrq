# rules: insecure-use-gets-fn
# languages: c
# from: c/lang/security/insecure-use-gets-fn.yaml

. as $root
| ( ($root | scan_ast(["*.c"]; [
      "gets($$$_)"
    ])) as $all
    | ( $all | of(["gets($$$_)"])
        | finding("insecure-use-gets-fn";
            "Avoid 'gets()'.") ) )
| report
