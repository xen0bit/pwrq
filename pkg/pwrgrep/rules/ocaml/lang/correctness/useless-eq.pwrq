# rules: useless-equal
# from: ocaml/lang/correctness/useless-eq.yaml

. as $root
| ( ($root | scan_ast(["*.ml"]; [
      "$X = $X"
    ])) as $all
    | ( $all | of(["$X = $X"])
        | finding("useless-equal";
            "This is always true.") ) )
| report
