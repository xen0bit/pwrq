# rules: lang-consistency-dockerfile
# from: yaml/semgrep/consistency/lang-consistency-dockerfile.yaml

. as $root
| ( ($root | scan_ast(["*.yaml"]; [
      "$X",
      "languages: $$$_"
    ])) as $all
    | ( $all | of(["$X"])
          | within($all | of(["languages: $$$_"]))
          | where_capture("X"; "^(docker)$")
        | finding("lang-consistency-dockerfile";
            "Found '$X' in language config which diverges from semgrep.dev normalization.") ) )
| report
