# rules: lang-consistency-solidity
# from: yaml/semgrep/consistency/lang-consistency-solidity.yaml

. as $root
| ( ($root | scan_ast(["*.yaml"]; [
      "$X",
      "languages: $$$_"
    ])) as $all
    | ( $all | of(["$X"])
          | within($all | of(["languages: $$$_"]))
          | where_capture("X"; "^(sol)$")
        | finding("lang-consistency-solidity";
            "Found '$X' in language config which diverges from semgrep.dev normalization.") ) )
| report
